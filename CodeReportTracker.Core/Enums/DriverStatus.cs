namespace CodeReportTracker.Core.Enums
{
    /// <summary>
    /// General driver availability states used for UI status binding.
    /// </summary>
    public enum DriverStatus
    {
        NotLoaded = 0,
        Busy = 1,
        Ready = 2
    }
}